java类修饰符就是用在class前面为了说明类的某些属性的符号。主要有以下几种：

public 公共类
，可被所有包中的类调用。
default 默认类（即class前什么都不假）
，可以被同一个包中的类和所有它的子类调用。
protected 保护类，只能被子类调用。
private 只能修饰内部类，一般不推荐使用，除了定义该类的类，其他类都不能调用。

abstract 抽象类
，不能直接生成实例，只能被继承。
fanal 最终类，不能被继承，只能生成实例。


static 静态类
，主要修饰内部类。
